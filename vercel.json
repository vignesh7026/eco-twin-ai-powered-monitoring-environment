{
  "buildCommand": "cd ecotwin-frontend && npm install && npm run build",
  "outputDirectory": "ecotwin-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
